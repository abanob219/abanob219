      - name: Generate Snake Animation
        uses: Platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: dist/github-contribution-grid-snake.svg
          color_snake: black
          color_dots: "#CCCCCC, #FFFFFF" # Light gray dots for visibility
          background: transparent

